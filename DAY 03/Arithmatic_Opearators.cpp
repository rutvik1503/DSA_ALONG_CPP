// Write a program of c++ program to perform the Arithmatic Operation

#include <iostream>
using namespace std;

int main()
{
    // Declare the variable with the integer data type
    int a=10, b=10, c;

    cout << endl;

    // it's print the addition of the a and b
    cout << a+b << endl;

    // it's print the substraction of the a and b
    cout << a-b << endl;

    // it's print the multiplication of the a and b
    cout << a*b << endl;

    // it's print the division of the a and b
    cout << a/b << endl;
    
    // it's print the module of the a and b
    cout << a%b << endl << endl;

    // Store the value of a + b in the variable c
    c = a+b ;

    // Print the value of the c
    cout << c << endl;

    cout << endl;
    
    return 0;
}